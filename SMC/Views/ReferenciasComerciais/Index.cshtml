@model IEnumerable<SMC.Models.Entidades.ReferenciasComerciais>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomeDaLoja)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Telefone)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomeDaLoja)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Telefone)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdReferenciaComercial }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdReferenciaComercial }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdReferenciaComercial })
        </td>
    </tr>
}

</table>
